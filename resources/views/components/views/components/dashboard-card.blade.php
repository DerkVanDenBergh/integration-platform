<div class="inline-block py-4 px-8 bg-white shadow-lg rounded-lg col-span-{{ $span }}">
    <div>
        <h2 class="text-gray-800 text-3xl text-green-400 font-bold">{{ $title }}</h2>
        <p class="mt-2 text-gray-600">{{ $description }}</p>
    </div>
    <div class="flex justify-end mt-4">
        <a href="{{ $link }}" class="text-xl font-medium text-indigo-400">{{ $caption }} ></a>
    </div>
</div>